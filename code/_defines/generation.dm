#define MIN_TUNNEL_LENGTH 5
#define AVERAGE_TUNNEL_LENGTH 3
#define MAX_TUNNEL_LENGTH 7
#define IN_BOUNDS(x, y) ((x >= 1) && (x < world.maxx) && (y >= 1) && (y < world.maxy))

#define POI_TUNNEL_POS "tunnel"
#define POI_ENTRANCE_POS "entrance"
#define POI_RANDOM_POS "random"